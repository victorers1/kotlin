// Package structure doesn't need to reflect folder structure
package packagea

fun main(){
    // function 'nome' belongs to the same package,
    // so it doesn't need to be imported
    println(nome())
}